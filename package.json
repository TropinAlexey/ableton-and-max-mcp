{
  "name": "ableton-and-max-mcp",
  "version": "2.1.0",
  "description": "Industrial-grade MCP server for Ableton Live + Max for Live (zero-latency)",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.js",
    "dev": "bun run --watch src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.4.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
